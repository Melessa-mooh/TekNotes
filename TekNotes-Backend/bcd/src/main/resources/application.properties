spring.application.name=bcd

# --- DATABASE CONFIGURATION ---
# Added maxAllowedPacket to allow large image transfers
spring.datasource.url=jdbc:mysql://localhost:3306/teknotes_db?useSSL=false&allowPublicKeyRetrieval=true&maxAllowedPacket=16777216
spring.datasource.username=root
#change this to your mysql password
spring.datasource.password=@Clyde1705
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA / HIBERNATE ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.port=8080

# --- JWT CONFIGURATION ---
jwt.secret=mySecretKeyForJWTTokenGenerationTekNotesApplicationSecure2024PleaseChangeInProduction
jwt.expiration=86400000

# --- FILE UPLOAD LIMITS (CRITICAL FIX) ---
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB